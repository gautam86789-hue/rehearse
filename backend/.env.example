PORT=5000
NODE_ENV=development

# Gemini Flash-Lite — the single AI provider for every feature (roleplay,
# scoring, story generation, coach chat, reply assistant). Falls back to an
# intelligent heuristic simulation if not provided or if a call fails.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.1-flash-lite

# Supabase (Optional for local memory mode)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=

# RevenueCat (For webhook verification)
REVENUECAT_WEBHOOK_SECRET=test_joNrjeYDDLoCfaZBBvRBwMwvKsh
# REST API Identifier=app2b9b63754a